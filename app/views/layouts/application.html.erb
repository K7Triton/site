<!DOCTYPE html>
<html>
<head>
  <title>Site</title>
  <%= stylesheet_link_tag "application", media: "all",
                          "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
  <!--[if lt IE 9]>
  <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
  <![endif]-->
</head>
<body>
<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="navbar-inner">
    <div class="container">
      <%= link_to "Social Site", '/', id: "logo" %>
      <div class="top_buttons">
      <nav>
        <ul class="nav pull-right">
          <% if current_user  %>
              <%= link_to "Find people", '/users', class: "btn btn-large btn-primary" %>
              <a href="/friendships/show" class="btn btn-primary" role="button">Friends</a>
              <%= link_to edit_user_path(current_user), method: :get do %> <span class="btn btn-primary">Edit profile</span>
                  <% end %>
              <a href="/logout" class="btn btn-primary" role="button">Logout</a>
          <% else %>
              <%= link_to "Sign up", 'users/new', class: "btn btn-large btn-primary" %>
              <a href="/login" class="btn btn-primary" role="button">Sign in</a>
          <% end %>
        </ul>
      </nav>
      </div>
    </div>
  </div>
</header>
<div class="container">
  <%= yield %>
</div>
</body>
</html>
